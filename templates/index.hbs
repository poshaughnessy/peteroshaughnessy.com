{{> header}}
<h1>Peter O'Shaughnessy</h1>
{{{ contents }}}
<ul>
  {{#each collections.posts}}
    <li>
      <h3>{{this.title}}</h3>
      <article>{{this.contents}}</article>
    </li>
  {{/each}}
</ul>
{{> footer}}
